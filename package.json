{
  "name": "ncc-ibm-validator",
  "version": "1.0.0",
  "description": "ncc test with ibm-openapi-validator",
  "scripts": {
    "dev": "ncc run index.ts",
    "build": "ncc build index.ts -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "chalk": "^2.4.1",
    "ibm-openapi-validator": "^0.30.0"
  },
  "devDependencies": {
    "@types/node": "^12.12.0",
    "@types/ibm-openapi-validator": "^0.15.0",
    "@zeit/ncc": "0.22.3",
    "typescript": "~4.0.5"
  }
}
